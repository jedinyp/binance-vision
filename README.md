# binance-vision
A Python tool for fetching, storing, and loading historical cryptocurrency data from https://data.binance.vision/. Supports configurable symbols, markets, and date ranges. The script populates an output folder with csv files based on the chosen parameters.
